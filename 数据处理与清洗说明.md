# 数据处理与清洗说明（2023 CUMCM C题 · 蔬菜类商品的自动定价与补货决策）

> 处理对象：`附件1~4.xlsx`；输出目录：`问题1_建模数据/`、`问题2和3_建模数据/`
> 全部 CSV 均为 **utf-8-sig** 编码，Excel 直接双击可正常显示中文。
> 复现脚本：`proc/build_data.py`（`python proc/build_data.py`）

---

## 一、数据概况与清洗规则

| 附件 | 原始规模 | 处理后去向 |
|---|---|---|
| 附件1 商品信息 | 251 单品 × 4 列 | 商品信息表 |
| 附件2 销售流水 | 878,503 行 | 过滤「退货」→ 日汇总 |
| 附件3 批发价格 | 55,982 行 | 原样 + 前向填充成本面板 |
| 附件4 损耗率 | 6 类均值 + 251 单品 | 合并为一张损耗率表 |

### 主要清洗决策
1. **编码统一**：四个附件单品种都含 251/246 个单品，编码是 15 位数字（无前导零），统一转成字符串规范形式用于跨表合并。
2. **剔除退货**：附件2 含「销售 878,042 行」与「退货 461 行」；所有退货行销量均为**负数**。销量/销售额汇总一律只用「销售」类型（471,275.839 kg / 3,372,023.34 元，与流水逐行加总完全一致）。
3. **单价口径**：同一单品同一天可能既有原价又有打折价（7076/46595 个“单品×日”存在价差）。汇总中的 **平均零售单价 = 销售额 ÷ 销量**（销量加权），不打折/打折笔数另列。
4. **缺失日（停业）**：2020-07-01~2023-06-30 共 1095 个日历日。附件2 缺 10 天（2021-02-11/12、2022-01-31、2023-01-21 等春节停业，及 2022-11~12 六天），附件3 缺 4 天。**没有把这些缺失日当作“销量 0”**：完整时序/宽表中停业日用 `NaN` 表示（避免把闭店误当无需求），营业日某品类确实无销售的才记为 0。营业与否见 `营业日历.csv`。
5. **批发价稀疏与成本填充**：附件3 每天只记录 28~79 个单品的批发价（均值 51），故每个（单品，日期）的**当日成本价 = 当天或此前最近一次批发价**（向前填充）。填充后全部 46,595 个“单品×销售日”都能匹配到成本（无缺失）。
6. **异常批发价**：原始批发价含极少量 <0.5 元/千克的记录（如 0.01，疑为录入错误，18 条 <0.1 元）。为不污染建模，数据未删除，而是加列 **`成本价异常标记`（=1 表示 <0.5 元）**，计算加成率/成本时建议先剔除再算。
7. **损耗率**：附件4 两个 sheet 用“小分类编码”对齐（与附件1 的分类编码一致）。给每个单品同时带上**单品损耗率**与**品类平均损耗率**。损耗率=0 的单品共 22 个（可能是预包装/干货），保留原值。
8. **有效成本（考虑损耗）**：为便于成本加成/利润测算，给出 `有效单位成本 = 批发成本 ÷ (1 − 单品损耗率)` 作为辅助列（买 q/(1−loss) 千克才卖得 q 千克）。

---

## 二、字段与口径（单位说明）

- 销量单位：**千克**；价格单位：**元/千克**；金额=销量×单价。
- `成本加成率 = 平均零售单价 / 当日(加权)成本价 − 1`。
- `打折销量占比 = 打折销量 ÷ 总销量`。
- `交易笔数`：销售流水条数（一次扫码算一笔）。
- 部分“品类×日”同时出现该品类当日无销（记为 0）与停业（NaN），使用时注意区分（见 `品类_日销售_完整面板.csv` 的 `是否营业_有销售` 列）。

---

## 三、问题1 → `问题1_建模数据/`

| 文件 | 行数 | 用途 |
|---|---|---|
| `商品信息表.csv` | 251 | 单品—分类对照（附件1） |
| `单品_日销售汇总.csv` | 46,595 | 日期×单品 销量/金额/笔数/打折/均价 → 单品销售规律 |
| `品类_日销售汇总.csv` | 6,474 | 日期×品类 汇总 + 在售单品数 → 品类规律 |
| `单品_总体统计.csv` | 246 | 3 年单品汇总（销售天数/总销量/日均/变异系数…）→ 分布/排名 |
| `品类_总体统计.csv` | 6 | 品类 3 年汇总 |
| `品类_日销售宽表.csv` | 1,095 | **行=全日历、列=6 品类**的销量矩阵（停业日 NaN）→ 品类相关/聚类 |
| `营业日历.csv` | 1,095 | 每天是否有销售/有批发价 → 时间序列补全 |

分析建议：品类相关可用宽表按周重采样后算相关；单品相关可在 `单品_日销售汇总.csv` 上按“重叠销售日”构造矩阵。

---

## 四、问题2、3 → `问题2和3_建模数据/`

| 文件 | 行数 | 用途 |
|---|---|---|
| `商品信息表.csv` | 251 | 单品字典（同问题1，两目录各放一份方便独立使用） |
| `损耗率表.csv` | 251 | 每单品：单品损耗率 + 品类平均损耗率（附件4 两 sheet 合并） |
| `单品_批发价原始.csv` | 55,982 | 附件3 原始（日期×单品×批发价） |
| `单品_批发价_前向填充面板.csv` | 176,797 | 每单品逐日“最近一次批发价”（成本面板），含 `是否原始记录` |
| `单品_日销售建模总表.csv` | 46,595 | **单品级主表**：日销量+均价+当日成本+损耗+加成率+有效成本（问题3 基础） |
| `品类_日销售汇总.csv` | 6,474 | **品类×日**（有销售日）：销量、零售均价、加权平均成本、加成率、品类均损（问题2 基础） |
| `品类_日销售_完整面板.csv` | 6,570 | 上述的完整日历版（1095 天×6 类，停业 NaN / 无销 0），补货时序建模直接可用 |
| `2023-06-24至30_单品日销售明细.csv` | 283 | 问题3 窗口期 7 天的单品×日明细 |
| `2023-06-24至30_可售品种清单.csv` | 49 | 窗口期在售 49 个品种：该周销量/均价/6-30 成本价/损耗率/异常标记（问题3 候选池） |

### 关键口径
- **问题2（品类）**：以 `品类_日销售_完整面板.csv` 的“销量”为需求历史预测未来 7 天日补货量；“加权平均成本 = Σ(单品销量×单品当日成本)/Σ销量”，零售均价=销售额/销量，据此分析成本加成与销量的关系。
- **问题3（单品）**：候选池为 `2023-06-24至30_可售品种清单.csv`（49 个，均能匹配到 6-30 当日或之前最近成本价）；每个单品的日销量、成本、损耗、加成率在 `单品_日销售建模总表.csv` 中逐日可得；需求预测后再乘以损耗放大得到订购量（订购量×损耗即损失部分）。

---

## 五、可复现性
- 脚本：`proc/build_data.py`（含全部清洗与加工逻辑，输出与本文一致）。
- 若需要“只保留清洗后的完整流水明细（约 87.8 万行，~100MB）”或其它粒度，可在此基础上调整后重跑。
